image: gitlab.dsi.universite-paris-saclay.fr:5005/l1infoinitiationsciencedonnees/computerlab/image:latest

variables:
  ASSIGNMENT: Semaine7
  STUDENT: $CI_PROJECT_ROOT_NAMESPACE

autograde:
  script:
    - STUDENT=`echo $STUDENT | sed -e 's/-travo//;s/-/./; s/L1InfoInitiationScienceDonnees/no.name/'`
    - travo student_autograde $ASSIGNMENT $STUDENT
  artifacts:
    paths:
      - autograded
      - feedback
    # reports:
    #   junit: feedback/scores.xml
